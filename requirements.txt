# TV Show Media Library Organizer Requirements

# Core dependencies
openai>=2.8.1  # Required for llm.py LLM functionality
pyyaml>=6.0.3  # Required for config.py YAML configuration loading
tmdbv3api>=1.9.0  # Required for tmdb.py TMDB API integration

# Web UI dependencies
fastapi>=0.121.3  # FastAPI web framework
uvicorn[standard]>=0.38.0  # ASGI server
python-multipart>=0.0.20  # For file uploads
pydantic>=2.12.4  # For request/response models

# Python 3.6+ required for:
# - pathlib (Path objects)
# - f-strings
# - dataclasses (Python 3.7+, or use typing.NamedTuple for 3.6)
# - enum.Enum

# Optional: For development/testing
# pytest>=6.0.0
# black>=21.0.0
# flake8>=3.8.0

# Note: Core organizer functionality uses only Python standard library modules:
# - os, pathlib - file system operations
# - re - regular expressions for pattern matching
# - shutil - file moving operations
# - argparse - command line interface
# - logging - colored output and progress tracking
# - typing, dataclasses, enum - type hints and data structures
# - json, datetime - utility functions